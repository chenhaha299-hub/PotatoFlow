# PotatoFlow 安全说明

PotatoFlow 采用本地优先的数据方式。公开仓库只应包含程序源码、通用示例和文档，不应包含任何用户任务、客户资料、浏览器导出、源文件、账号凭据或访问密钥。

## 禁止提交的内容

- `.env`、`.env.*` 和本地运行配置
- OpenAI、GitHub、Cloudflare、Apple 或其他平台的 API Key、Token
- 私钥、证书、设备授权码和登录 Cookie
- `.potatoflow/`、浏览器导出、完整备份和真实项目 JSON
- 用户姓名、邮箱、手机号、私人路径、IP 地址和客户资料

只有明确用于公开说明的 `.env.example` 可以进入仓库，其中必须使用无效占位值。

## 提交前检查

1. 运行 `python -m unittest tests.test_secrets -v`。
2. 检查 `git status`，确认没有 `.env`、备份、导出或个人文件。
3. 查看待提交差异，确认没有真实账号、路径和业务数据。
4. 推送后查看 GitHub 的 Security 页面和 Actions 结果。

## 如果密钥已经泄露

1. 立即在对应平台撤销或轮换密钥，不要只删除 GitHub 文件。
2. 从当前代码中删除密钥并检查相关日志。
3. 清理 Git 历史中的密钥记录，再重新推送修复后的历史。
4. 检查是否存在异常调用、费用或未知登录。
5. 不要在 Issue、提交信息或聊天截图中再次粘贴完整密钥。

历史清理不能让已经泄露的密钥重新安全。撤销或轮换始终是第一步。
